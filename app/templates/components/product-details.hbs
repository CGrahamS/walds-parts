<div class="well">
  <h3>{{product.name}}</h3>
  <h5>{{product.price}}</h5>
  <img src={{product.image}} alt="{{product.name}}" class="img-responsive"/>
  <h5>{{product.rating}}</h5>
  <p>
    {{product.description}}
  </p>

</div>
