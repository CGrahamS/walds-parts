
import { lowStock } from 'walds-parts/helpers/low-stock';
import { module, test } from 'qunit';

module('Unit | Helper | low stock');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lowStock([42]);
  assert.ok(result);
});

